import { NodeSpec } from '../types/node';
import { ContainerItem, DockerImageItem } from '../types/docker';
import { TelemetryPoint, ProcessItem, HealthStatusMatrix } from '../types/monitoring';
import { FileItem } from '../types/file';
import { FirewallRule, SecretItem } from '../types/security';
import { AuditLogEvent } from '../types/auth';
import { CatalogItem } from '../types/catalog';
import { AutomationWorkflow, QueueJob } from '../types/workflow';

export const mockNodes: NodeSpec[] = [];
export const mockContainers: ContainerItem[] = [];
export const mockTelemetry: TelemetryPoint[] = [];
export const mockProcesses: ProcessItem[] = [];
export const mockFiles: FileItem[] = [];
export const mockFirewallRules: FirewallRule[] = [];
export const mockSecrets: SecretItem[] = [];
export const mockCatalog: CatalogItem[] = [];
export const mockWorkflows: AutomationWorkflow[] = [];
export const mockQueueJobs: QueueJob[] = [];
export const mockAuditLogs: AuditLogEvent[] = [];
export const mockHealthMatrix: HealthStatusMatrix[] = [];
